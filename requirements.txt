requests>=2.31.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
schedule>=1.2.0
tenacity>=8.2.0
backtrader>=1.9.78.123  # backtesting framework

# TA-Lib: high-performance C-based indicator library (optional).
# Installation varies by environment:
#   pip    : pip install TA-Lib  (requires pre-built C library on the system)
#   conda  : conda install -c conda-forge ta-lib   ← recommended on Windows/macOS
# If installed, indicators can be replaced with talib.RSI(), talib.SMA(), etc.
# for faster computation on large OHLCV datasets.
# TA-Lib>=0.4.28
