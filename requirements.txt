requests>=2.31.0
pykrx>=0.6.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
schedule>=1.2.0
tenacity>=8.2.0
backtrader>=1.9.78.123  # backtesting framework
fastapi>=0.110.0        # lightweight web dashboard
uvicorn[standard]>=0.29.0  # ASGI server for FastAPI
pytest>=7.4.0           # test runner (also in requirements-dev.txt)
pytest-cov>=4.1.0       # coverage reporting

# scikit-learn: used by strategy/hedge_logic.py predict_volatility() (Phase 6 ML hedge).
scikit-learn>=1.4.0

# TA-Lib: high-performance C-based indicator library.
# Installation varies by environment:
#   pip    : pip install TA-Lib  (requires pre-built C library on the system)
#   conda  : conda install -c conda-forge ta-lib   ← recommended on Windows/macOS
# If TA-Lib is not installed, strategy/indicators.py falls back to the
# pure pandas/numpy implementation automatically (no code change required).
TA-Lib>=0.4.28
